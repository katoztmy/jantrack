services:
  postgres:
    image: postgres:16
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: jantrack
      POSTGRES_PASSWORD: jantrack
      POSTGRES_DB: jantrack
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
